# Workflow schemas

TaskNotes Workflows writes the canonical Runtime profile 0.2
`runtime_workflow` record defined by `@callumalpass/mdbase-runtime`. The plugin
validates every generated record with that package before writing it.

`tasknotes-workflow.schema.json` documents the compatibility input accepted for
files created by TaskNotes Workflows 0.1.x. It is not an authoring target.
